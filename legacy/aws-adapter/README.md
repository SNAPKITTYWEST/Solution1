# Original Solution1 source

These three files are preserved from the initial local project before the sovereign runtime was added. They are not built by `Solution1.sln` and retain their original AWS SDK dependency for provenance. Their source comments are historical claims, not verified feature guarantees.

The original two `.cs` files also remain at the root of `Sovereign.Bedrock/` and are excluded by that project's `Compile Remove`. Active implementations are in `Sovereign.Bedrock/Runtime/` and have no AWS SDK dependency.
